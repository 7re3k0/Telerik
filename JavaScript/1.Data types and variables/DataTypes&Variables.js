// Array
var games = ['LoL', 'HotS', 'GW2'];
console.log(games);

// Booleans
var a = 7;
var b = 3;
var c = (a + b);
var d = (a - b);
var compare = (d > c);
console.log (compare);

// Integers
var numberA = 30;
var numberB = 70;

// Float
var floating = 30.7;

// Object
var object = {name: 'Fundamentals', coarseType: 'JavaScript'};

// Strings
var str = 'Suuuup';

// Function
var func = function(){console.log;};

// Null
var nullValue = null;

// Undefined
var undeFind = undefined;

// Quoted String
var quote = 'This is the single \' Quote \' \n and this is the doubel \" Quote \"';

// Type of
console.log(typeof(games));
console.log(typeof(a));
console.log(typeof(floating));
console.log(typeof(object));
console.log(typeof(str));
console.log(typeof(func));

// Task 4
console.log('Type of ' + nullValue + ' is: ' + typeof(nullValue));
console.log('Type of ' + undeFind + ' is: ' + typeof(undeFind));